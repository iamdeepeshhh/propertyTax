## Copy to .env and adjust values as needed

# Database connection
DB_HOST=localhost
DB_NAME=trialarvi
DB_USER=postgres
DB_PASSWORD=root

# App server port (host port when using docker-run)
SERVER_PORT=8080

